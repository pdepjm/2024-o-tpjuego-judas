import wollok.game.*

import example.*

program defenderBase {
    game.height(9)
    game.width(16)
    game.boardGround("cortado2.png")
    game.cellSize(50)
    game.title("Defender la Base")

    interfaz.empezarJuego()

    game.start()
}